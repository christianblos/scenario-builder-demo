package com.example.demo.scenario.builder;

/**
 * Defines the order in which the scenario builder parts are executed (from top to bottom).
 */
public enum BuilderPartOrder {

	PLAYER,
	RESOURCE,
	ALLIANCE,
	CITY,
	BUILDING,
	PRODUCTION,
	TRADE_OFFER,

}
